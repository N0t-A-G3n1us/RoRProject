<%= form_with(model: invite_request, local: true) do |form| %>
  <% if invite_request.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(invite_request.errors.count, "error") %> prohibited this invite_request from being saved:</h2>

      <ul>
      <% invite_request.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
